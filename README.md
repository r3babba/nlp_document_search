# nlp_document_search

This program uses a document corpus to train word embeddings and has the ability to take an input search query and find similar sentences in the corpus using word embeddings and cosine similarity.
